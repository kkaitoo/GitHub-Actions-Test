name: Run workflow_run after Pytest

on: 
  workflow_run:
    workflows: [RunPytest]
    types: 
      - completed
  workflow_dispatch:

jobs:
  hello-workflow-run:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: | 
          echo "Hello This is workflow_run"
      - name: Checkout repository
        uses: actions/checkout@v4
